# Architecture Traceability Index

> **Last Updated**: 2026-07-23 (Slice Propagation batch — see §Slice Propagation 2026-07-23)
> **Engine**: Godot 4.7-stable
> **Source of truth**: `tr-registry.yaml` (machine-readable, stable IDs) — this file is the
> human-readable rollup. Regenerated by `/architecture-review` write phase.

## Coverage Summary

- Total requirements (2026-07-11 review baseline + same-day rerun): **349**
- Covered: **349 (100 percent)** - Partial: 0 - Gaps: **0**
- **Back-annotation complete (2026-07-11): 471 registered TR-IDs, all labeled inline
  in their GDDs (1:1 verified — every GDD label has a registry entry and vice versa).**
  The count exceeds the 349 baseline because the back-annotation pass normalized
  restatement occurrences and extracted at finer granularity.
- ADRs: 14 (13 Accepted, ADR-0003 Superseded by ADR-0014)
- Verdict trail: baseline CONCERNS (all-Proposed) -> rerun **PASS** after spike QQ3,
  full ADR resolution, and the ADR-0014 large-world supersede
  (see `architecture-review-2026-07-11.md` + `-rerun.md`)

## ADR-worthy TR-IDs (the original 63 of 471 registered)

ADR<->TR mapping lives in each ADR's "GDD Requirements Addressed" section; the registry
carries the stable IDs and requirement text (471 entries total after the 2026-07-11
back-annotation — see the Back-Annotation section below). The tables below list only
the 63 ADR-addressed entries.

### scene-world-management (6)

| TR-ID | Requirement |
|---|---|
| TR-scene-world-management-004 | Boot gate: Resource & Item Database must reach Ready before the Valley scene attaches and before Building System / Villager AI initialize; DB failure halts b... |
| TR-scene-world-management-013 | Savepoint creation is bound exclusively to transition_ended(success=true). |
| TR-scene-world-management-023 | Boot-order enforcement is owned by a dedicated boot-order mechanism/ADR. |
| TR-scene-world-management-024 | Savepoint creation is bound exclusively to transition_ended(success=true). |
| TR-scene-world-management-030 | All transition tunables are read from data/config, never hardcoded literals. |
| TR-scene-world-management-033 | Two-simultaneous-live-scenes partitioning for WorldEnvironment, Camera3D.current, audio listeners, navigation maps, input routing, and GI/lighting bleed — sh... |

### voxel-world (5)

| TR-ID | Requirement |
|---|---|
| TR-voxel-world-017 | Raycast/cell picking without the per-cell-collider scalability trap. |
| TR-voxel-world-018 | Raycast/cell picking without the per-cell-collider scalability trap. |
| TR-voxel-world-021 | Save/Load iteration API exposes only occupied (non-empty) cells. |
| TR-voxel-world-023 | World/terrain tuning values are read from config, never hardcoded. |
| TR-voxel-world-025 | The voxel rendering representation is an architecture decision (resolved by ADR-0014: chunked mesher; formerly open per ADR-0003). |

### camera-input (2)

| TR-ID | Requirement |
|---|---|
| TR-camera-input-019 | All tuning-knob values are read from config/exported vars, not hardcoded. |
| TR-camera-input-020 | Exactly one owner per click; Camera & Input must not also emit a world-action signal when a Control consumed the click. |

### time-tick-system (1)

| TR-ID | Requirement |
|---|---|
| TR-time-tick-system-020 | All tunables are read from config, not hardcoded. |

### resource-item-database (4)

| TR-ID | Requirement |
|---|---|
| TR-resource-item-database-007 | Validation produces a structured result returned by the validation call itself (dependency-injectable/unit-testable), not just logged. |
| TR-resource-item-database-010 | Returned item definitions are immutable to callers; caller mutation must never affect subsequent queries. |
| TR-resource-item-database-019 | Boot sequencing: Scene/World Management's Booting state gates on this database reaching Ready before Building System / Villager AI initialize; no dependency ... |
| TR-resource-item-database-023 | visual_asset reference must resolve to an existing asset at boot; unresolved references fail boot validation. |

### building-system (10)

| TR-ID | Requirement |
|---|---|
| TR-building-system-002 | Pick->preview->commit pipeline requires a raycast/cell query every frame a tool is armed. |
| TR-building-system-003 | Ghost-preview re-rasterization, degrade-to-outline threshold, and aggregate ghost rendering. |
| TR-building-system-023 | Save/Load: open blueprint cells + per-cell construction progress are persisted. |
| TR-building-system-026 | Pick->preview->commit pipeline requires a raycast/cell query every frame a tool is armed. |
| TR-building-system-033 | Save/Load: the undo/redo stack is explicitly excluded from save data. |
| TR-building-system-035 | Ghost-preview re-rasterization, degrade-to-outline threshold, and aggregate ghost rendering. |
| TR-building-system-038 | All tuning knobs must be externally data-driven, never hardcoded. |
| TR-building-system-039 | Ghost-preview re-rasterization, degrade-to-outline threshold, and aggregate ghost rendering. |
| TR-building-system-040 | Mid-path solidification race: intra-frame ordering between a villager position update and a same-frame occupancy check must be made deterministic. |
| TR-building-system-041 | Ghost-preview re-rasterization, degrade-to-outline threshold, and aggregate ghost rendering. |

### villager-ai-behavior (11)

| TR-ID | Requirement |
|---|---|
| TR-villager-ai-behavior-009 | Standability predicate (canonical walkability ground truth). |
| TR-villager-ai-behavior-010 | Step-legality predicate (canonical walkability ground truth). |
| TR-villager-ai-behavior-011 | Walkability rules exposed as the canonical ground truth queried by Build Validation. |
| TR-villager-ai-behavior-013 | Deciding-pass staggering: simultaneous mass-Deciding events stagger selection passes across frames in a stable, deterministic villager order. |
| TR-villager-ai-behavior-016 | Dependency-injected RNG source (live RNG in production, fixed-sequence double in tests) for deterministic wander/micro-behavior selection. |
| TR-villager-ai-behavior-027 | Save/Load: serialize per-villager position, activity, claimed job id, owned bed, need levels; re-validate stale claim/bed ids on load without crashing. |
| TR-villager-ai-behavior-035 | The pathfinding algorithm itself is an open architectural choice. |
| TR-villager-ai-behavior-036 | Mid-travel re-path required when a Voxel World write blocks the current path. |
| TR-villager-ai-behavior-041 | Milestone-gated performance: 16.6 ms frame budget maintained at VS population (~5) and Full Vision ceiling (30). |
| TR-villager-ai-behavior-046 | Mid-path solidification race also affects F4 nudge-aside targeting and walled-in queries. |
| TR-villager-ai-behavior-047 | Pre-VS performance spike: synthetic 30-villager stress case covering staggered Deciding. |

### build-validation-navigability (4)

| TR-ID | Requirement |
|---|---|
| TR-build-validation-navigability-008 | Must reuse Villager AI's exact walkability rules, not a plain 4/8-neighbor flood-fill. |
| TR-build-validation-navigability-009 | Graph walk from region interior to any open-sky standable cell. |
| TR-build-validation-navigability-016 | Config-load-time blocking validation: max_room_height >= Building System's max wall_height. |
| TR-build-validation-navigability-019 | Deferred engine-capability decision: NavigationServer3D rebake vs custom cell flood-fill. |

### needs-mood-system (6)

| TR-ID | Requirement |
|---|---|
| TR-needs-mood-system-020 | Config-load-time blocking validation of the ladder-ordering invariant. |
| TR-needs-mood-system-025 | Live-pair integration-test infrastructure: a real Needs instance + real Villager AI villager exercised through a full decay->recovery->wake round trip. |
| TR-needs-mood-system-026 | Save/Load: per-villager need values persisted (compound cluster 026-029 in ADR-0012; individual split pending GDD back-annotation). |
| TR-needs-mood-system-027 | Save/Load: smoothed mood restored as-saved, never re-initialized via the F4 spawn formula (compound cluster 026-029 in ADR-0012). |
| TR-needs-mood-system-028 | Save/Load: schema-drift handling on load (compound cluster 026-029 in ADR-0012). |
| TR-needs-mood-system-029 | Save/Load: restore exactly the saved need/mood state without re-derivation (compound cluster 026-029 in ADR-0012). |

### building-ui (9)

| TR-ID | Requirement |
|---|---|
| TR-building-ui-008 | Material/furniture palette populated at runtime from Resource & Item Database queries. |
| TR-building-ui-014 | First-appearance grace on expiry must re-query Build Validation's queryable state, not the triggering event. |
| TR-building-ui-015 | Grace/debounce/invalid-cue timers pause and resume-with-remaining-time only on the Suspended transition, decoupled from Control visibility. |
| TR-building-ui-018 | Dismissal debounce: per-key wall-clock suppression window. |
| TR-building-ui-028 | Hover-over-HUD sets a queryable flag that gates world-pick starts. |
| TR-building-ui-029 | Hover suppression must not swallow the pointer-release event of an in-progress world-space drag. |
| TR-building-ui-037 | All numeric UI-behavior values must be externally data-driven config, not hardcoded. |
| TR-building-ui-038 | Mirror/event logic must be exercisable headless via mocked upstream signals and mocked queryable state (BLOCKING automated-test tier). |
| TR-building-ui-040 | Timer-architecture decision: centralized expiry-timestamp manager vs N per-issue Godot Timer nodes. |

### villager-info-ui (5)

| TR-ID | Requirement |
|---|---|
| TR-villager-info-ui-002 | Villager-hit query must honor the same shared world-pick hover-suppression flag defined by Building UI. |
| TR-villager-info-ui-014 | Villager-hit testing is a dedicated physics query on a dedicated villager collision layer, fully separate from block-picking; Building's placement raycast mu... |
| TR-villager-info-ui-015 | Villager-vs-block pick resolution: nearest wins by ray-parametric distance, with an explicit tie tolerance constant pick_tie_epsilon. |
| TR-villager-info-ui-016 | Multiple-villagers-along-ray: nearest wins deterministically; equal-distance ties break via stable villager processing order. |
| TR-villager-info-ui-023 | Panel logic must support headless mocking of villager hits, tool-arm state, and all six activity states (BLOCKING automated-test tier). |

## Known Gaps

None — 0 uncovered requirements as of the 2026-07-11 rerun.

## Back-Annotation Task (carried condition from the gate check) — DONE 2026-07-11

The back-annotation pass is complete. All 11 system GDDs carry inline
`[TR-<system>-NNN]` labels; the registry (`tr-registry.yaml`) is the source of
truth for every requirement text and holds **471 entries** (63 original + 28
previously ADR-referenced-but-unregistered + 380 newly extracted). The tables
above list only the original 63 ADR-worthy entries; per-system totals after
back-annotation:

| System | TR-IDs |
|---|---|
| building-system | 73 |
| villager-ai-behavior | 64 |
| build-validation-navigability | 51 |
| needs-mood-system | 44 |
| building-ui | 44 |
| resource-item-database | 37 |
| scene-world-management | 34 |
| voxel-world | 34 |
| camera-input | 32 |
| villager-info-ui | 30 |
| time-tick-system | 28 |

Notes from the pass: the phantom `TR-camera-input-010` was dropped (not
registered); the needs-mood compound cluster 026-029 was split into four
individually anchored requirements (029's text corrected — per-need STATE is
deliberately re-derived from restored values, values themselves restored
exactly); several registry texts were aligned to GDD wording
(`revised: 2026-07-11`), including resolution updates for TR-villager-ai-035/046,
TR-building-ui-040, TR-build-validation-019, and TR-scene-world-management-033.
/create-stories can now embed stable TR-IDs for every requirement.

## Superseded Requirements

- TR-voxel-world-025 (rendering representation) — resolved by ADR-0014 (formerly an
  open decision under ADR-0003); text revised 2026-07-11, ID unchanged.

## Slice Propagation 2026-07-23

Vertical-slice change batch (see `change-impact-2026-07-23-slice-batch.md`). New
slice-batch TR-IDs and their governing ADRs; no existing IDs renumbered.

| TR-ID | Requirement (summary) | ADR |
|---|---|---|
| TR-voxel-world-050 | Floor terrain-replace stores original terrain as `restore_value`; reversible, never left empty | ADR-0014 (write path); ADR-0012 (serialized state) |
| TR-voxel-world-051 | Terrain dig orders remove the 1..5 terrain-band/sand family (excl. water/trunk/leaves) via the standard batched write + `cells_removed` signal | ADR-0007 (incremental nav patch); ADR-0014 |
| TR-voxel-world-052 | Mesher hard requirement: CW winding + backface culling ENABLED (CCW+`CULL_DISABLED` is an expired slice mitigation) | ADR-0014 (Decision §2) |
| TR-villager-ai-behavior-099 | Unstuck watchdog: deterministic teleport rescue of a Traveling/Working villager after a stuck-tick threshold | ADR-0009 (2nd sanctioned discrete mutation); ADR-0008 |
| TR-villager-ai-behavior-100 | Rescue-target selection: expanding-ring BFS, F2 lexicographic tie-break, bounded radius | ADR-0009; ADR-0007 (shared `is_standable`) |
| TR-villager-ai-behavior-102 | Seal prevention: build-write refusal that would entrap a villager (self-seal exception self-heals via watchdog) | ADR-0009 (occupancy consumer) |

**ADR disposition this batch:** ADR-0009, ADR-0010, ADR-0014 amended in place
(Status Accepted retained); ADR-0012, ADR-0013 carry recorded impact notes gated on
pending **ADR-0015** (Large-World Storage & Residency, options analysis pending user
choice). **ADR-0016** (Project/Building Entity Lifecycle) reserved, authored separately.
Tick rate → 4.0 ticks/s is a GDD/config change owned by `time-tick-system.md` (parallel
agent) — touches no ADR. Registry text for the new TR-IDs to be appended to
`tr-registry.yaml` by the next `/architecture-review` write phase.
