# Unsupported Legacy Features

This plugin intentionally does not provide equivalents for Claude-specific
notification hooks, status-line scripts, or custom-agent registration. It uses
skills, role profiles, and `AGENTS.md` instructions instead.
