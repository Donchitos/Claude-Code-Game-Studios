# Smoke Check — rid-009 (MVP data content: tier-0 set + bed)

**Date**: 2026-07-27
**Scope**: Story rid-009 — the project's first authored `res://data/items/` content.
**Owner**: godot-gdscript-specialist

---

## 1. What was run

1. Class-cache rebuild: `Godot_v4.7-stable_win64.exe --headless --editor --path . --quit` — clean.
2. Full headless GdUnit4 suite (`tests/run-tests.cmd`, `res://tests/unit` + `res://tests/integration`),
   run in the foreground, twice for reproducibility.
3. A throwaway content-query suite (`tests/_scratch_rid009_content_smoke_test.gd`, run once, then
   deleted — per this sprint's own DoD line keeping rid-009's content read in the smoke artifact,
   not the permanent unit suite) querying the REAL `ResourceItemDatabase` Autoload directly.

## 2. Final suite numbers

**1418 test cases · 0 errors · 0 failures · 0 flaky · 0 skipped · 0 orphans · exit code 0**
(121/121 suites) — reproduced identically across two independent full runs.

Baseline: 1383 (Sprint 10 actuals). Growth this tree: +35 (from rid-007/rid-008, landed earlier
this sprint; this story renamed 2 test functions and edited 1 in place — net 0 new test cases from
rid-009 itself).

## 3. Smoke-1..4 (this story's own QA Test Cases) — content check against SHIPPED data

| # | Check | Query | Result |
|---|---|---|---|
| Smoke-1 | Tier-0 building materials exactly 3 | `list_ids_by_tier(0) ∩ list_ids_by_category(&"building_material")` == `["wood_block", "stone_block", "thatch_block"]` | **PASS** |
| Smoke-2 | Category isolation + list-all | `list_ids_by_category(&"building_material")` == the 3 materials only; `list_ids_by_category(&"furniture_fixture")` == `["bed"]` only; `list_all_ids()` == all 4, `"missing_item"` absent | **PASS** |
| Smoke-3 | Bed footprint | `get_by_id(&"bed").get_footprint()` == `Vector2i(1, 2)` | **PASS** |
| Smoke-4 | All fields populated, boot green | All 4 entries: non-empty id/display_name/category/storage_category, non-null `visual_asset`, `tier == 0`, `max_stack_size >= 1`; `ResourceItemDatabase.is_ready() == true` | **PASS** |

Note on Smoke-1: RID's own `list_ids_by_tier` is a raw one-axis query (Core Rule 8); `bed` is also
authored at `tier = 0` (MVP's sole furniture entry, always available, no unlock system exists yet —
matching the `rid-008` fixture convention of `tier = 0` on furniture test fixtures too), so "tier-0
building materials" (GDD AC14) is the caller-side intersection of tier and category, not a raw
tier-only query. Confirmed both queries independently return the expected sets before intersecting.

## 4. The three-condition GREEN crown (Sprint 11 QA plan, rid-009 dedicated section)

1. **Tier-0 family coverage passes** — `_check_tier0_family_coverage()` raised no BLOCKING issue;
   proven by the whole database resolving `success = true` (any coverage gap would have failed the
   ENTIRE batch, per `_load_definitions()`'s "never launch with a partially valid database"
   contract, which would have cascaded into every assertion below failing). **PASS**.
2. **Boot reaches ACTIVE** — `boot_spine_integration_test.gd ::
   test_real_boot_zero_injected_modules_still_reaches_active` (real `GameWorld`, real Autoload RID,
   never a mock) — **PASS**.
3. **`is_need_functional(&"bed")` is TRUE against the real Autoload** — proven by the three flipped
   `build_validation` tests plus the direct scratch content query. **PASS**.

See `production/qa/evidence/rid-009-bed-functional-flip-evidence.md` for the full quoted
before/after and the non-vacuity demonstration.

## 5. Sequencing check (F2)

**Not yet satisfied by commit history at the time of this smoke check** — all four `.tres` entries
(3 materials + bed) were authored and validated together in this working session, and this
implementer does not commit (parent does, per this task's own instruction). **Action required at
commit time**: the three tier-0 materials (`wood_block.tres`, `stone_block.tres`,
`thatch_block.tres`) plus their mesh assets must be committed FIRST, verified booting green on their
own, and `bed.tres` + `bed_mesh.tres` committed SEPARATELY, second — never a lone `bed.tres` in
isolation in the commit history. Flagged here explicitly per this plan's own F2 sequencing gate,
rather than silently assumed satisfied.

## 6. AC29 (Advisory, material-picker legibility) — deferred, not exercised

Requires a windowed session with a real GPU and a human viewer; out of reach for this headless
implementation pass. Per the sprint's own named fallback ("if meshes are not ready by story close,
ship rid-009 content anyway... record explicitly if the fallback was exercised"): the fallback WAS
exercised — the four `visual_asset` bindings are primitive `BoxMesh` + `StandardMaterial3D`
placeholders carrying the correct Visual Direction Note colours (Timber Brown / Hearth-stone Grey /
Thatch Umber / Hearth Gold), not real `.vox`/Blender-pipeline art. `production/qa/evidence/
rid-tier0-palette-evidence.md` remains outstanding — not created this pass.

## 7. Verdict

**PASS.** Full suite green (1418/1418, 0 orphans, exit 0, reproduced twice), all 4 content smoke
checks pass against shipped data, and the three-condition crown definition holds against real
production data. **One open action for hand-off**: commit the tier-0 materials and `bed.tres` as
two separate commits (materials first) to satisfy F2's sequencing requirement in the commit
history itself. AC29's advisory screenshot remains outstanding (non-blocking).
