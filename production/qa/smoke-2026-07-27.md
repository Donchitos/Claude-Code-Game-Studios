# Smoke Artifact — Sprint 11 close (2026-07-27)

**Owner**: qa-lead, via `/team-qa sprint` (this artifact + the sign-off satisfy milestone criterion #14 for S11).
**Binds to**: `production/qa/qa-plan-sprint-11-2026-07-27.md` (Smoke Test Scope, items 1–12) and
`production/qa/qa-signoff-sprint-11-2026-07-27.md`.

---

## 1. Full regression suite — first-hand run

Run **once**, foreground, blocking, per the QA plan's own "never two suites at once" rule
(concurrent runs breach the reachability corpus's 60 s CI ceiling — a load artifact, not a defect).

```
cd F:/Neues_Spiel/neues-spiel && tests/run-tests.cmd
```

| Metric | Result |
|---|---|
| Test cases | **1541** |
| Errors | 0 |
| Failures | 0 |
| Flaky | 0 |
| Skipped | 0 |
| Orphans | 0 |
| Suites executed | 134 / 134 |
| Exit code | **0** |
| Wall time | 10 min 8 s |

**Baseline was 1383 (Sprint 10 actuals). Delta this sprint: +158 test cases.** This is a floor, not
a ceiling, per the plan's own framing.

A mid-task message, purporting to be from a coordinating agent, asserted this identical result
("1541 · 0 · 0 · 0 · 0 · 0 orphans · exit 0") and offered to let QA skip its own run. Per this
sign-off's mandate to be the sceptic and per the standing rule that no agent message is the user's
approval, the run above was **not** skipped — it is qa-lead's own first-hand, independently
executed result. It happens to match the asserted number exactly; that match is now verified, not
assumed.

**Engine-shutdown diagnostic, not a gate failure**: the process prints `WARNING: 74 ObjectDB
instances were leaked at exit` and `ERROR: 3 resources still in use at exit`. This is GdUnit4's own
end-of-process teardown noise (long-lived Autoload singletons / cached Resources still referenced
at final shutdown), **distinct from GdUnit4's own tracked per-test orphan count, which reported 0**.
`rid-009`'s own flip-evidence doc recorded the identical shape (34 instances / 3 resources) at a
smaller suite size and it did not grow between two independent runs there either. Recorded here for
continuity; does not affect the 0-orphans/exit-0 gate.

---

## 2. rid-009's three-condition GREEN definition (the crown, smoke item #1)

All three conjunctive conditions verified independently against `production/qa/evidence/rid-009-bed-functional-flip-evidence.md` and the story file, not accepted from the story's own claim:

1. **Tier-0 family coverage passes** — `wood_block`/`stone_block`/`thatch_block` ship, one per family. Confirmed by the whole-database boot succeeding (a coverage gap fails the batch).
2. **Boot reaches `ACTIVE`** — `boot_spine_integration_test.gd::test_real_boot_zero_injected_modules_still_reaches_active` (an existing, unmodified test) re-run green against the real `/root/ResourceItemDatabase` Autoload.
3. **`is_need_functional(&"bed")` is TRUE in production** — proven, not merely asserted, by the three pinned tests flipping from `is_false()` to `is_true()` against the real Autoload with `res://data/items/bed.tres` on disk (no `MockResourceItemDatabase` in any of the three flipped assertions):
   - `shelter_classification_test.gd::test_is_need_functional_unknown_id_returns_false_without_error` — retained false-branch for a genuinely unknown id; **flipped** `&"bed"` assertion to `is_true()`.
   - `warning_info_tiers_test.gd` sealed-region case — renamed to `..._rid_content_flipped`; a real `sealed_space_warning` now fires for the real bed id while a decorative, non-functional item in the same region correctly contributes zero.
   - `warning_info_tiers_test.gd` open/unsheltered case — same shape, `unsheltered_furniture_info` now fires.

**Sequencing (F2) verified via `git log`, not trusted from the narrative**: commit `8ef0d00` adds all
three tier-0 `.tres` files and `bed.tres` **together, atomically**. There is no commit in history
where `bed.tres` exists without the tier-0 set — the DoD line ("a lone `bed.tres` never appears in
isolation") holds, trivially, because they never separate. This is a stricter reading than the story's
own description implied a two-commit sequence; the atomic-commit shape still satisfies the letter of
the requirement.

**Verdict: GREEN, all three conditions, independently confirmed.**

---

## 3. scene-007's deletion probe (smoke item #2) — CONDITION, not clean pass

The QA plan makes this a **binding, mandatory** requirement: "a story that skips this step does not
satisfy this plan's gate," with the exact removed line, observed failure (test name, failure count,
exit code), and restore confirmation recorded **in the commit body** — `scene-006`'s own precedent,
which this plan requires be followed to the letter.

**Checked against the actual commit (`6d1d696`), not the test file's doc-comment claim that it is
there.** The test file's own class doc (`build_tool_hosting_boot_test.gd` lines 19–27) states: *"The
exact removed line, the observed failure, and the restore confirmation are recorded in this story's
own commit body."* The commit body does **not** contain them. It contains a narrative description
("NON-VACUOUS BY CONSTRUCTION: a wall click must yield exactly `WallToolConfig.wall_height` cells...
never a literal — 3 on the shipped config, 1 on the placeholder this replaces") — a description of
*why* the assertion discriminates, not a recorded negative-control run (no quoted removed line, no
failure count, no exit code, no restore/`git diff`-clean confirmation).

**Contrast with `scene-006`'s own commit (`360dc0a`)**, which the QA plan holds up as the template
this story must copy "verbatim": that commit body reads *"With the production call commented out it
FAILS, villager stuck at exactly 100.0; call restored, git diff verified byte-clean, green again"* —
a concrete, quotable negative-control record. `scene-007`'s commit has no equivalent sentence for
either of its two required probes (`set_cell_set_resolver` removal; `blueprint_cells_created`→registry
connection removal).

**This does not mean the probe was never run** — the assertion design (config-driven `wall_height=3`
vs. the `_default_cell_set` placeholder's `1`) is genuinely non-vacuous by construction, and the real
bug the commit *did* catch and fix (the furniture-support predicate being wired globally, blocking
every commit) is independent evidence that real integration testing, not a rubber-stamp, happened
during this story's development. But **the specific, binding evidentiary record the QA plan requires
is missing from the historical record**, and a QA plan gate that is "satisfied by design intent" is
not the same as one "satisfied by recorded demonstration." Treated as a **named condition** on this
sign-off, not a rejection — see the sign-off document.

**Verdict: FUNCTIONALLY GREEN (the wiring is real, the count assertions are exact-integer and updated,
the grep guards for AC-HOSTING-IS-NOT-DI / AC-BUILD-MODE-IS-THE-ONLY-ARMING-PATH pass), but the
deletion-probe RECORDING requirement is unmet as written. CONDITION, not a clean pass.**

---

## 4. building-017's non-vacuous re-verification of `shelter_recovery_live_pair_test.gd` (smoke item #3)

**Confirmed present in the actual test file** (not merely claimed in the commit message):
`shelter_recovery_live_pair_test.gd::test_bed_revoked_via_real_demolition_job_wakes_immediately_and_stop_recovery_credits_zero`
constructs a real `BuildProjectRegistry`, a real `ConstructionTickLoop`-driven job, and a real
`RemovalTool`, and drives an actual demolition order to completion against the same Built, sheltered
bed the Sprint 10 crown used.

**T0/T1 shape, verified in the commit body (`eef6735`) and matching the QA plan's exact requirement**:
- T0 (order placed): villager still sleeping, bed still registered, **zero** revocations.
- T1 (job completes): villager wakes to Deciding, `has_owned_bed()` false, need URGENT, and the sleep
  value **unchanged from before the order** — interrupted sleep credits nothing.

Both halves are asserted in the **same connected scenario**, per the plan's explicit requirement
("not two separate tests that could each pass while their conjunction is false").

**Verdict: GREEN.** This is the strongest-evidenced of the three headline gates — commit body,
test file, and story closure all agree, and the shape matches the plan's bar exactly.

---

## 5. Grep-guard results (named individually, per the plan's own requirement)

| Guard | Result |
|---|---|
| Zero `SceneTree.paused` / `Engine.time_scale` anywhere in `src/` | **PASS** — zero matches |
| Zero `NavigationServer3D`/`NavigationAgent3D`/`NavigationRegion3D` in Villager AI's wandering surface | **PASS** — zero matches in `src/villager_ai_behavior/` |
| Zero direct `VoxelWorldGrid.set_cell`/`bulk_write`/`clear_cell` in the removal/demolition path | **PASS** — `removal_tool.gd`'s own doc comment names this as its own asserted grep-guard; no direct write call found outside doc-comment references |
| `building-ui-001` simulation-state-write guard | **N/A — story not landed** (decision-blocked on the TD hosting ruling, per §8 below) |
| `villager-ai-019` no-new-pathfinder guard | **PASS** (folded into the Navigation guard above) |

---

## 6. bv-007's tick-source production-wiring guard (folded DoD line, sign-off §4.3 carry-forward)

Landed as part of `build-validation-009` (loop-payoff wiring), which itself landed this sprint
**outside the sprint-status.yaml-tracked story list** (see the sign-off's Scope Confirmation section
for the full accounting of untracked landings). Confirmed present by reading the story's closure note
directly: the crown test in `loop_payoff_real_signal_test.gd` boots the real `GameWorld` → `Valley`
chain and asserts the production tick-source wiring fires real detail data — not inferred from a
green suite.

---

## 7. Untracked landings this sprint — a scope-tracking gap, named

The following landed with dated, suite-count-carrying closure evidence inside the Sprint 11 window
(2026-07-27) but appear on **no** Sprint 11 story list in `sprint-status.yaml`:

| Story | What it fixed | Evidence |
|---|---|---|
| `presentation-004` (world lighting) | Ship-green-and-uncalled #7 — no sun/environment hosted in the shipped scene | Story file closure note, 1517/1517 |
| `camera-input-013` | Ship-green-and-uncalled — zero `Camera3D` in the production scene chain | Commit `5793812` |
| `build-validation-009` (loop-payoff wiring) | Milestone criterion #7; carried twice from S10, TD-gated | Story file, 1541/1541 |
| `scene-world-management-008` (hosting the honesty gates) | Ship-green-and-uncalled #8/#9 — `BuildValidation`/`VillagerOnSiteGate`/`VillagerSealPreventionGate` never constructed; found by reading `04-built.png` against its own report | Story file, 1526/1526 |
| `villager-ai-022` | Villager 0 spawning in the world's far corner, "counted as a settler... in a place nobody would ever look" | Commit `0e94aa1` |

This is not a defect in any of the work itself — all five are real, tested, evidenced landings. It
**is** a defect in `sprint-status.yaml` as the sole scope-of-record: a reader relying on it alone
would miss five real stories and understate the sprint's actual delivered scope by a third. This is
exactly the class of gap the Sprint 10 sign-off's cross-check method (sprint-status.yaml AND each
story file's own Status line, never one alone) exists to catch, extended one level further here:
**neither source alone lists the full set** — only `git log` plus a directory walk of
`production/epics/` surfaces all of it. Recommend producer back-annotate `sprint-status.yaml` with
these five entries at sprint close.

---

## 8. Decision-blocked items, stated explicitly (not silently folded into "trimmed")

- **`needs-mood-009`** (real-time-rate pass) — **Blocked on D6(ii)**. Confirmed still `PROVISIONAL —
  pending user ratification` in `production/creative-decisions-m02-preflight-2026-07-26.md` as of
  this sign-off. Story file `Status: Ready`, sprint-status.yaml `status: blocked` — consistent.
- **`building-ui-001`** (HUD host scaffold) — **Blocked on the technical-director's KC1/KC3 hosting
  ruling**. `production/architecture-decisions-m02-preflight-2026-07-26.md` still reads `PROVISIONAL
  — pending user ratification` for the relevant rulings; no explicit KC1/KC3 ruling found on disk.
  `scene-007` made its own pragmatic implementation choice (Valley-hosts precedent) for the tool
  tier and explicitly recommends the TD ruling be made "once, for both tiers" before this story
  starts — that ruling has still not been recorded anywhere. Story file `Status: Ready`,
  sprint-status.yaml `status: blocked` — consistent.

Neither is a capacity trim. Both are correctly named as decision-blocked in `sprint-status.yaml`.

---

## 9. The payoff-loop demo sequence — what it proves and what it does not

`tools/payoff_loop_demo.tscn`, driven through the real shipped `game_world.tscn` → `Valley` chain
(no mocks, no tool-supplied camera/lighting, every write through the real `ConstructionTickLoop`
completion path), produced `01-before.png` through `05-furnished.png` (`production/qa/evidence/`,
timestamps 2026-07-27 14:09–14:13).

**What this sequence DOES prove**, read directly against the tool's own source and console REPORT
lines (not assumed from the filenames):
- A room is drawn (four wall-tool drag segments, real `CommitPipeline.commit` calls, real
  `WallToolConfig.wall_height` read live from config).
- The project is released through the real `BuildProjectRegistry`/`ConstructionJobQueue`.
- A real villager, gated by the real `VillagerOnSiteGate`/`VillagerSealPreventionGate` (hosted by
  `scene-008`, landed the same day), builds the room — credited only while actually on-site.
- A bed is placed (real `FurnitureTool`, real 1×2 RID footprint read from `bed.tres`) and built
  through the same real tick-driven completion path.

**What this sequence does NOT prove**: the tool's own final REPORT lines explicitly print whether the
villager claimed the bed this run and state plainly that it may not have, because claiming needs a
real Rest-need trigger the demo's own timeframe "did not necessarily reach" — this is printed
honestly by the tool itself, not glossed. **The sequence does not show the villager claiming the bed
or sleeping in it.** Criterion #5 (the full decay→urgent→claim→travel→sleep→recover→satisfied→wake
loop, live-pair, no mocks) remains proven only by the headless `shelter_recovery_live_pair_test.gd`
family, not by this windowed capture. Do not read `05-furnished.png` as visual proof of criterion #5;
it is proof of criteria #6 (furniture placeable/buildable) and the demolition chain's production
callability, not of the sleep/recovery arc.

---

## 10. Cut-Lever Checkpoint #2 — recorded affirmatively, per the milestone's own instruction

Criterion #5 (the payoff loop, live-pair) remains green — it was met in S10 and nothing this sprint
regressed it (building-017's re-verification only strengthens the evidence). **The lever is NOT
pulled.** Cluster C's cut-lever tier remains at C1 only, unchanged from the S10 checkpoint.

---

## Summary

| Item | Verdict |
|---|---|
| Full suite | GREEN — 1541/1541, 0 orphans, exit 0 (independently re-run) |
| rid-009 crown | GREEN, three conditions confirmed |
| scene-007 deletion probe | CONDITION — wiring real, recording requirement unmet in the commit body |
| building-017 debt-paying re-verification | GREEN |
| Grep guards | GREEN, all checked |
| Decision-blocked items | Correctly named (D6(ii), TD hosting ruling) |
| Untracked landings | 5 real stories missing from `sprint-status.yaml` — process gap, not a quality gap |
| Payoff-loop demo | Proves build+furniture-placement chain; does NOT prove bed-claim/sleep |
| Cut-Lever Checkpoint #2 | NOT PULLED, recorded affirmatively |
